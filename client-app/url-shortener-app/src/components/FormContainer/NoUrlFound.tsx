import * as React from 'react';

interface INoUrlFoundProps {
  
}

const NoUrlFound: React.FunctionComponent<INoUrlFoundProps> = (props) => {
  return (
    <div>
      Hello World
    </div>
  );
};

export default NoUrlFound;